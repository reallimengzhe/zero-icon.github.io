version: "2.0"

services:
  zero-icon:
    ports:
      - 80
    expose:
      - 80
    resources:
      cpu: 0.1
      mem: 128
    deployments:
      replicas: 1

# 中间件
addons:
  monitor:
    plan: monitor:standard
# environments:
#   development:
#     envs:
#       SPRING_PROFILES_ACTIVE: dev
#   test:
#     envs:
#       SPRING_PROFILES_ACTIVE: test
#   staging:
#     envs:
#       SPRING_PROFILES_ACTIVE: pre
#   production:
#     envs:
#       SPRING_PROFILES_ACTIVE: prod
